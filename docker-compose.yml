version: '3.7'

services:
  go-server:
    image: golang:bookworm
    container_name: go-server
    ports:
      - "8080:8080"
    volumes:
      - .:/go/src/app
    working_dir: /go/src/app
    command: sleep infinity
